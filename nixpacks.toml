[phases.setup]
nixPkgs = ["python310", "qpdf", "libffi", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn hr_api.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
